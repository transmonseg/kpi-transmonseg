# KPI 100% pela API (beta) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Gerar o KPI do beta a partir das paradas da API do Unitrac (sem PDF), consolidando os eventos GPS crus em entregas, validado contra o PDF antes de virar tela.

**Architecture:** Uma engine pura (`consolida.ts`) transforma os eventos crus de `/mapa_servicos/stops` nas mesmas `UnitracParadaRow` que o matcher já consome — então matcher/preview/status/confirmação são 100% reaproveitados. Fase 1 prova a viabilidade via comparador (gate ≥95% entregas, ±5min). Fase 2 (só se passar) liga um toggle `fonte: pdf|api` na rota e tela beta. Produção (`/api/kpi/simples`) nunca é tocada.

**Tech Stack:** TypeScript, Next.js (nodejs runtime), vitest, API datalayer Unitrac (`src/lib/unitrac-api/`).

---

## Contexto pra quem nunca viu o código

- **Convenção de horário:** todos os parsers gravam BRT mascarado como UTC (ex: 05:51 BRT vira `2026-06-12T05:51:00Z`). O spike confirmou que o campo `_data` da API JÁ vem nessa convenção (o evento `2026-06-12T05:51:36Z` casou a entrega das 05:51 do PDF). Então NÃO se converte timezone — usa `_data` direto.
- **Shape que o matcher consome** (`src/lib/kpi/matcher.ts:247`):
  ```ts
  export type UnitracParadaRow = {
    id: string; placa_norm: string; chegada: string; saida: string | null
    duracao_seg: number | null; local_parada: string; codigo_loja: string | null
    nome_loja: string | null; lat: number | null; lng: number | null
    endereco?: string | null; classificacao: string; ordem: number
  }
  ```
  `classificacao` é string livre; os valores usados são `'BASE'`, `'LOJA'`, `'FORA_BASE'`, `'FAKE_EXIT'`.
- **Geofences da API** (`src/lib/unitrac-api/pontos.ts`): `acharLojaPorCoordenada(lat, lon, pontos)` devolve a geofence autoritativa (`{ cod, nome, lat, lon, raio }`) que contém a coordenada, ou null.
- **Base Benassi:** fica em ~`-22.8290, -43.3420` (visto nos eventos crus e nos testes do matcher).
- **Raw da API:** `GET /mapa_servicos/stops/{cv}/{horas}` → `{ paradas: [{ _data, tempoparada, latitude, longitude }] }`. O `buscarParadas` atual filtra `tempoparada >= 120` — a engine NÃO pode filtrar (as entregas vêm como eventos curtos).
- **Rodar testes:** `npx vitest run src/lib/unitrac-api/<arquivo>.test.ts`. Suíte toda: `npx vitest run`.

---

## Estrutura de arquivos

| Arquivo | Responsabilidade |
|---|---|
| `src/lib/unitrac-api/consolida.ts` (criar) | engine: eventos crus → `UnitracParadaRow[]`; helpers `buscarStopsCru`, `clusteriza`, `consolidaParadasApi` |
| `src/lib/unitrac-api/consolida.test.ts` (criar) | TDD da engine |
| `src/lib/unitrac-api/index.ts` (modificar) | exportar a engine |
| `scripts/dev/comparar-api-vs-pdf.mts` (criar) | comparador — GATE da Fase 1 |
| `src/app/api/kpi/beta/route.ts` (modificar) | param `fonte: 'pdf'\|'api'` (Fase 2) |
| `src/app/painel/kpi/beta/page.tsx` (modificar) | toggle Fonte (Fase 2) |

---

## FASE 1 — Engine + prova (make-or-break)

### Task 1: Tipo e fetch cru (sem filtro de 120s)

**Files:**
- Create: `src/lib/unitrac-api/consolida.ts`
- Test: `src/lib/unitrac-api/consolida.test.ts`

- [ ] **Step 1: Escrever o teste que falha**

```ts
// src/lib/unitrac-api/consolida.test.ts
import { describe, it, expect } from 'vitest'
import { clusteriza, type StopApiCru } from './consolida'

describe('clusteriza', () => {
  it('agrupa eventos próximos no espaço e separa os distantes', () => {
    const ev: StopApiCru[] = [
      { _data: '2026-06-12T05:51:00Z', tempoparada: 60, latitude: -22.90316, longitude: -43.11053 },
      { _data: '2026-06-12T05:55:00Z', tempoparada: 60, latitude: -22.90320, longitude: -43.11050 }, // ~5m do 1º
      { _data: '2026-06-12T06:42:00Z', tempoparada: 60, latitude: -22.90721, longitude: -43.10379 }, // ~850m → outro cluster
    ]
    const cl = clusteriza(ev)
    expect(cl).toHaveLength(2)
    expect(cl[0].eventos).toHaveLength(2)
    expect(cl[1].eventos).toHaveLength(1)
  })
})
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `npx vitest run src/lib/unitrac-api/consolida.test.ts`
Expected: FAIL — `clusteriza` não existe / módulo não encontrado.

- [ ] **Step 3: Implementar o tipo e o clusteriza**

```ts
// src/lib/unitrac-api/consolida.ts
import { apiGet } from './client'
import { haversine } from '@/lib/utils/geo'
import { acharLojaPorCoordenada, type MapaPontos } from './pontos'
import type { UnitracParadaRow } from '@/lib/kpi/matcher'

export type StopApiCru = { _data: string; tempoparada: number; latitude: number; longitude: number }

export type BaseCoord = { lat: number; lng: number }
export const BASE_BENASSI: BaseCoord = { lat: -22.8290, lng: -43.3420 }
export const RAIO_BASE_M = 500      // dentro disso da base → classificacao BASE
export const RAIO_CLUSTER_M = 150   // eventos a ≤150m do âncora = mesma permanência
export const MIN_DUR_SEM_GEO_SEG = 300 // cluster sem geofence só vira parada se ≥5min

/** Eventos crus da API, SEM o filtro de 120s (as entregas vêm fragmentadas). */
export async function buscarStopsCru(cv: string, horas: number): Promise<StopApiCru[]> {
  const d = (await apiGet(`/mapa_servicos/stops/${cv}/${horas}`)) as { paradas?: StopApiCru[] } | null
  return (d?.paradas ?? []).filter(p => p.latitude != null && p.longitude != null && Math.abs(p.latitude) > 1)
}

export type Cluster = { eventos: StopApiCru[]; lat: number; lng: number }

/** Agrupa eventos consecutivos (por tempo) que ficam a ≤RAIO_CLUSTER_M do âncora
 *  (1º evento do grupo) numa "permanência" no mesmo local. */
export function clusteriza(eventos: StopApiCru[]): Cluster[] {
  const ord = [...eventos].sort((a, b) => new Date(a._data).getTime() - new Date(b._data).getTime())
  const clusters: Cluster[] = []
  for (const e of ord) {
    const atual = clusters[clusters.length - 1]
    if (atual && haversine(atual.lat, atual.lng, e.latitude, e.longitude) <= RAIO_CLUSTER_M) {
      atual.eventos.push(e)
    } else {
      clusters.push({ eventos: [e], lat: e.latitude, lng: e.longitude })
    }
  }
  return clusters
}
```

- [ ] **Step 4: Rodar o teste e ver passar**

Run: `npx vitest run src/lib/unitrac-api/consolida.test.ts`
Expected: PASS (1 teste).

- [ ] **Step 5: Commit**

```bash
git add src/lib/unitrac-api/consolida.ts src/lib/unitrac-api/consolida.test.ts
git commit -m "feat(consolida): clusteriza eventos GPS crus da API + fetch sem filtro"
```

---

### Task 2: Consolidação — cluster → UnitracParadaRow (geofence + classificação + saída)

**Files:**
- Modify: `src/lib/unitrac-api/consolida.ts`
- Test: `src/lib/unitrac-api/consolida.test.ts`

- [ ] **Step 1: Escrever os testes que falham**

```ts
// adicionar em src/lib/unitrac-api/consolida.test.ts
import { consolidaParadasApi } from './consolida'
import type { MapaPontos } from './pontos'

const pontos: MapaPontos = {
  '3030113': { cod: '3030113', nome: 'SUPERPRIX LJ 13 - NITEROI', lat: -22.90327, lon: -43.11062, raio: 200 },
  '3030011': { cod: '3030011', nome: 'SUPERPRIX LJ 10 - ICARAÍ', lat: -22.907512, lon: -43.103701, raio: 300 },
}

describe('consolidaParadasApi', () => {
  it('FUM-8748: 2 entregas, saída inferida do próximo cluster, geofence resolvida', () => {
    const ev: StopApiCru[] = [
      { _data: '2026-06-12T05:51:00Z', tempoparada: 60, latitude: -22.90316, longitude: -43.11053 }, // Niterói
      { _data: '2026-06-12T06:42:00Z', tempoparada: 60, latitude: -22.90721, longitude: -43.10379 }, // Icaraí
    ]
    const ps = consolidaParadasApi(ev, pontos, '2026-06-12', 'FUM8748')
    expect(ps).toHaveLength(2)
    expect(ps[0].codigo_loja).toBe('3030113')
    expect(ps[0].classificacao).toBe('LOJA')
    expect(ps[0].chegada).toBe('2026-06-12T05:51:00.000Z')
    expect(ps[0].saida).toBe('2026-06-12T06:42:00.000Z') // saída = chegada do próximo cluster
    expect(ps[1].codigo_loja).toBe('3030011')
  })

  it('classifica BASE quando o cluster está dentro do raio da base Benassi', () => {
    const ev: StopApiCru[] = [
      { _data: '2026-06-12T04:00:00Z', tempoparada: 600, latitude: -22.8291, longitude: -43.3421 },
    ]
    const ps = consolidaParadasApi(ev, pontos, '2026-06-12', 'FUM8748')
    expect(ps).toHaveLength(1)
    expect(ps[0].classificacao).toBe('BASE')
    expect(ps[0].codigo_loja).toBeNull()
  })

  it('filtra eventos de outro dia', () => {
    const ev: StopApiCru[] = [
      { _data: '2026-06-11T05:51:00Z', tempoparada: 600, latitude: -22.90316, longitude: -43.11053 },
    ]
    expect(consolidaParadasApi(ev, pontos, '2026-06-12', 'FUM8748')).toHaveLength(0)
  })

  it('descarta cluster curto SEM geofence (blip de trânsito), mantém o longo', () => {
    const ev: StopApiCru[] = [
      { _data: '2026-06-12T05:00:00Z', tempoparada: 30, latitude: -22.95, longitude: -43.20 }, // blip 30s, sem geo
      { _data: '2026-06-12T06:00:00Z', tempoparada: 600, latitude: -22.80, longitude: -43.50 }, // 10min, sem geo → mantém
    ]
    const ps = consolidaParadasApi(ev, pontos, '2026-06-12', 'FUM8748')
    expect(ps.map(p => p.classificacao)).toEqual(['FORA_BASE'])
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/unitrac-api/consolida.test.ts`
Expected: FAIL — `consolidaParadasApi` não existe.

- [ ] **Step 3: Implementar consolidaParadasApi**

```ts
// adicionar em src/lib/unitrac-api/consolida.ts

/** Eventos crus → paradas no formato que o matcher consome.
 *  - filtra a janela do dia (`_data` já vem em BRT mascarado como UTC)
 *  - clusteriza permanências
 *  - saída = chegada do PRÓXIMO cluster (o caminhão "saiu" ao aparecer noutro lugar);
 *    no último cluster, saída = último evento + sua duração
 *  - resolve geofence autoritativa; classifica BASE/LOJA/FORA_BASE
 *  - descarta cluster curto sem geofence (blip de trânsito) */
export function consolidaParadasApi(
  eventos: StopApiCru[],
  pontos: MapaPontos,
  data: string,
  placaNorm: string,
  baseCoord: BaseCoord = BASE_BENASSI,
): UnitracParadaRow[] {
  const doDia = eventos.filter(e => e._data.slice(0, 10) === data)
  const clusters = clusteriza(doDia)
  const out: UnitracParadaRow[] = []
  let ordem = 0
  for (let i = 0; i < clusters.length; i++) {
    const c = clusters[i]
    const ultimo = c.eventos[c.eventos.length - 1]
    const chegada = c.eventos[0]._data
    const proximo = clusters[i + 1]
    const saida = proximo
      ? proximo.eventos[0]._data
      : new Date(new Date(ultimo._data).getTime() + (ultimo.tempoparada ?? 0) * 1000).toISOString()
    const durSeg = Math.round((new Date(saida).getTime() - new Date(chegada).getTime()) / 1000)

    const naBase = haversine(baseCoord.lat, baseCoord.lng, c.lat, c.lng) <= RAIO_BASE_M
    const geo = naBase ? null : acharLojaPorCoordenada(c.lat, c.lng, pontos)
    const classificacao = naBase ? 'BASE' : geo ? 'LOJA' : 'FORA_BASE'

    // blip de trânsito: cluster curto, sem geofence e fora da base → descarta
    if (!naBase && !geo && durSeg < MIN_DUR_SEM_GEO_SEG) continue

    ordem++
    out.push({
      id: `${placaNorm}-api-${ordem}`,
      placa_norm: placaNorm,
      chegada: new Date(chegada).toISOString(),
      saida: new Date(saida).toISOString(),
      duracao_seg: durSeg,
      local_parada: geo ? geo.nome : naBase ? 'BASE BENASSI - BASE BENASSI' : 'FORA DE BASE E LOCAL DE SERVICO',
      codigo_loja: geo?.cod ?? null,
      nome_loja: geo?.nome ?? null,
      lat: c.lat,
      lng: c.lng,
      endereco: null,
      classificacao,
      ordem,
    })
  }
  return out
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/lib/unitrac-api/consolida.test.ts`
Expected: PASS (todos os testes da engine).

- [ ] **Step 5: Exportar no barrel e checar tipos**

Modify `src/lib/unitrac-api/index.ts` — adicionar após a linha `export * from './confirma'`:

```ts
export * from './consolida'
```

Run: `npx tsc --noEmit 2>&1 | grep "^src/" | head` → Expected: sem saída.

- [ ] **Step 6: Commit**

```bash
git add src/lib/unitrac-api/consolida.ts src/lib/unitrac-api/consolida.test.ts src/lib/unitrac-api/index.ts
git commit -m "feat(consolida): cluster→UnitracParadaRow com geofence, BASE e saída inferida"
```

---

### Task 3: Comparador API vs PDF — o GATE da Fase 1

**Files:**
- Create: `scripts/dev/comparar-api-vs-pdf.mts`

> Este script é a prova. Roda a consolidação da API e compara com o PDF do mesmo
> dia. NÃO é teste unitário — é validação de viabilidade com dados reais.

- [ ] **Step 1: Escrever o comparador**

```ts
// scripts/dev/comparar-api-vs-pdf.mts
import { readFile } from 'fs/promises'
import { config } from 'dotenv'
config({ path: '.env.local' })
import { buscarFrota } from '../../src/lib/unitrac-api/frota.ts'
import { buscarPontos } from '../../src/lib/unitrac-api/pontos.ts'
import { buscarStopsCru, consolidaParadasApi } from '../../src/lib/unitrac-api/consolida.ts'
import { parseUnitracPdfJs } from '../../src/lib/parsers/unitrac-pdf-pdfjs.ts'
import { haversine } from '../../src/lib/utils/geo.ts'

// USO: npx tsx scripts/dev/comparar-api-vs-pdf.mts <caminho-pdf> <YYYY-MM-DD> [horas]
const [pdfPath, data, horasArg] = process.argv.slice(2)
const horas = Number(horasArg ?? 48)
if (!pdfPath || !data) { console.error('uso: comparar-api-vs-pdf.mts <pdf> <YYYY-MM-DD> [horas]'); process.exit(1) }

const hhmm = (iso: string) => new Date(iso).toISOString().slice(11, 16)
const entregaPdf = (p: any) => p.classificacao === 'LOJA' || p.classificacao === 'FORA_BASE'

const buf = await readFile(pdfPath)
const resumos = await parseUnitracPdfJs(buf, null)
const frota = await buscarFrota()
const pontos = await buscarPontos(frota.map(v => v.cv))

let totalPdf = 0, reproduzidas = 0, horarioOk = 0, lojaOk = 0
for (const r of resumos) {
  const v = frota.find(x => x.placaNorm === r.placa_norm)
  const entregasPdf = (r.paradas ?? []).filter(entregaPdf)
  if (entregasPdf.length === 0) continue
  const eventos = v ? await buscarStopsCru(v.cv, horas) : []
  const api = consolidaParadasApi(eventos, pontos, data, r.placa_norm).filter(p => p.classificacao !== 'BASE')

  for (const pdfP of entregasPdf) {
    totalPdf++
    // casa a entrega do PDF com a da API mais próxima no espaço (≤300m) e tempo (≤30min)
    const m = api.find(a =>
      a.lat != null && pdfP.lat != null &&
      haversine(a.lat, a.lng!, pdfP.lat, pdfP.lng) <= 300 &&
      Math.abs(new Date(a.chegada).getTime() - new Date(pdfP.chegada).getTime()) <= 30 * 60_000)
    if (!m) { console.log(`  ❌ ${r.placa_norm} ${hhmm(pdfP.chegada)} ${pdfP.codigo_loja ?? pdfP.local_parada} — API não reproduziu`); continue }
    reproduzidas++
    if (Math.abs(new Date(m.chegada).getTime() - new Date(pdfP.chegada).getTime()) <= 5 * 60_000) horarioOk++
    if (pdfP.codigo_loja && m.codigo_loja === pdfP.codigo_loja) lojaOk++
    else if (!pdfP.codigo_loja) lojaOk++
  }
}
const pct = (n: number) => totalPdf ? Math.round((n / totalPdf) * 100) : 0
console.log(`\n=== GATE (data ${data}) ===`)
console.log(`Entregas no PDF: ${totalPdf}`)
console.log(`Reproduzidas pela API: ${reproduzidas} (${pct(reproduzidas)}%)  [meta ≥95%]`)
console.log(`Chegada ±5min: ${horarioOk} (${pct(horarioOk)}%)`)
console.log(`Identidade de loja correta: ${lojaOk} (${pct(lojaOk)}%)`)
console.log(reproduzidas / Math.max(totalPdf,1) >= 0.95 && horarioOk / Math.max(totalPdf,1) >= 0.95 ? '\n✅ GATE PASSOU' : '\n⚠️ GATE NÃO passou — calibrar RAIO_CLUSTER_M / MIN_DUR_SEM_GEO_SEG / inferência de saída')
```

- [ ] **Step 2: Rodar contra um dia real**

Run: `NODE_OPTIONS="--max-old-space-size=4096" npx tsx scripts/dev/comparar-api-vs-pdf.mts "C:/Users/media/Downloads/relatorio_10254.pdf" 2026-06-12 48`
Expected: imprime as métricas do gate. Anotar o resultado.

- [ ] **Step 3: Calibrar se o gate não passar**

Se `Reproduzidas < 95%` ou `Chegada ±5min < 95%`, ajustar as constantes em `consolida.ts` (`RAIO_CLUSTER_M`, `MIN_DUR_SEM_GEO_SEG`) e/ou a inferência de saída, re-rodar o comparador e os testes unitários até o gate passar. Cada ajuste mantém os testes da Task 1/2 verdes (`npx vitest run src/lib/unitrac-api/consolida.test.ts`).

- [ ] **Step 4: Commit (registra o resultado do gate)**

```bash
git add scripts/dev/comparar-api-vs-pdf.mts src/lib/unitrac-api/consolida.ts
git commit -m "feat(consolida): comparador API vs PDF (gate Fase 1) + calibração"
```

- [ ] **Step 5: DECISÃO DE GATE**

Se o gate passou (≥95% reproduzidas + ≥95% chegada ±5min em ≥1 dia real completo): seguir pra Fase 2. Se NÃO passou após calibração razoável: PARAR e reportar ao usuário com os números — a ideia precisa de mais investigação no endpoint (ex: paginação, parâmetro de data) antes de virar tela. Não construir a Fase 2 com gate reprovado.

---

## FASE 2 — Toggle no beta (SÓ se o gate da Fase 1 passou)

### Task 4: Param `fonte: 'pdf' | 'api'` na rota beta

**Files:**
- Modify: `src/app/api/kpi/beta/route.ts`

> Hoje a rota baixa o PDF do storage e monta `veiculosMap` (≈ linhas 307-358).
> Quando `fonte === 'api'`, montar os veículos via consolidação em vez do PDF.

- [ ] **Step 1: Ler o body com `fonte`**

Em `src/app/api/kpi/beta/route.ts`, no destructure do body (procurar `skipSave = false } = body as {`), adicionar `fonte`:

```ts
  const { escalaBucketPath, escalaBucketPaths, unitracBucketPath, unitracBucketPaths, data, alteracoes = [], lineEdits = [], skipSave = false, fonte = 'pdf' } = body as {
    // ...campos existentes...
    fonte?: 'pdf' | 'api'
  }
```

- [ ] **Step 2: Tornar o PDF opcional quando fonte==='api'**

Localizar o guard que exige PDF (`const temPdf = rawUnitracPaths.some...` seguido do `if (!temPdf) return new NextResponse(...)`). Envolver a exigência só no modo PDF:

```ts
  const temPdf = rawUnitracPaths.some(p => p.toLowerCase().endsWith('.pdf'))
  if (fonte === 'pdf' && !temPdf) {
    return new NextResponse('Suba o relatório Unitrac em PDF (formato principal). XLSX é opcional como fallback.', { status: 400 })
  }
```

- [ ] **Step 3: Montar veículos pela API quando fonte==='api'**

Logo ANTES do bloco que monta `veiculosMap` a partir do PDF (a linha `const veiculosMap = new Map<...`), inserir o caminho da API. Importar no topo: de `@/lib/unitrac-api` adicionar `buscarStopsCru, consolidaParadasApi` aos imports já existentes (`buscarFrota`, `buscarPontos` já estão); e o tipo `import type { ResumoVeiculo, ClassificacaoParada } from '@/lib/types/unitrac'`. A shape exata exigida (de `src/lib/types/unitrac.ts`):

```
ResumoVeiculo = { placa_norm, placa_raw, inicio_viagem: Date|null, fim_viagem: Date|null, qtd_paradas, paradas: ParadaUnitrac[], saida_cd: Date|null }
ParadaUnitrac = { placa_norm, chegada: Date, saida: Date, duracao_seg, distancia_km: number|null, endereco: string|null, lat, lng, local_parada, codigo_loja, nome_loja, classificacao: ClassificacaoParada, ordem }
```

Implementação (preenche TODOS os campos; `saida_cd`/`inicio_viagem` ficam null — o matcher recomputa saída-CD sozinho):

```ts
  // KPI BETA — fonte API: paradas vêm da consolidação dos eventos crus do Unitrac,
  // sem PDF. Reaproveita exatamente o mesmo matcher/preview adiante.
  const veiculosMap = new Map<string, ResumoVeiculo>()
  if (fonte === 'api') {
    const frotaApi = await buscarFrota()
    const pontosApi = await buscarPontos(frotaApi.map(v => v.cv))
    const placasEscala = new Set(escalaLinhas.map(l => l.placa_norm).filter(Boolean) as string[])
    for (const v of frotaApi) {
      if (!placasEscala.has(v.placaNorm)) continue
      const eventos = await buscarStopsCru(v.cv, 48)
      const paradas = consolidaParadasApi(eventos, pontosApi, data, v.placaNorm)
      if (paradas.length === 0) continue
      veiculosMap.set(v.placaNorm, {
        placa_norm: v.placaNorm,
        placa_raw: v.placa,
        inicio_viagem: null,
        fim_viagem: null,
        qtd_paradas: paradas.length,
        saida_cd: null,
        paradas: paradas.map(p => ({
          placa_norm: v.placaNorm,
          chegada: new Date(p.chegada),
          saida: new Date(p.saida!),
          duracao_seg: p.duracao_seg ?? 0,
          distancia_km: null,
          endereco: p.endereco ?? null,
          lat: p.lat,
          lng: p.lng,
          local_parada: p.local_parada,
          codigo_loja: p.codigo_loja,
          nome_loja: p.nome_loja,
          classificacao: p.classificacao as ClassificacaoParada,
          ordem: p.ordem,
        })),
      })
    }
  } else {
    // ... bloco existente que baixa o PDF e popula veiculosMap (NÃO alterar) ...
  }
```

> Nota de implementação: o bloco `else` recebe TODO o código existente de parse do
> PDF (o `for (const unitracPath of unitracPaths)` e o merge XLSX+PDF). Recortar o
> bloco atual pra dentro do `else`, sem alterar a lógica dele. Atenção: a declaração
> `const veiculosMap = ...` que existe hoje deve ser REMOVIDA do lugar antigo (ela
> passa a ser a declaração acima, antes do `if`).

- [ ] **Step 4: Verificar tipos**

Run: `npx tsc --noEmit 2>&1 | grep "^src/" | head`
Expected: sem saída. Se `ResumoVeiculo` exigir campos extras (ex `motorista`, `cv`), preencher com os valores disponíveis (`null`/`v.cv`) — conferir o tipo em `src/lib/types/unitrac.ts` e ajustar o objeto.

- [ ] **Step 5: Rodar a suíte (produção intocada segue verde)**

Run: `npx vitest run`
Expected: todos passam (a rota não tem teste unitário; o objetivo é garantir que nada quebrou).

- [ ] **Step 6: Commit**

```bash
git add src/app/api/kpi/beta/route.ts
git commit -m "feat(kpi-beta): fonte 'api' monta paradas via consolidação (sem PDF)"
```

---

### Task 5: Toggle "Fonte: PDF | API" na tela beta

**Files:**
- Modify: `src/app/painel/kpi/beta/page.tsx`

- [ ] **Step 1: Estado do toggle**

Perto dos outros `useState` do componente principal da página beta (procurar `const [data, setData]` ou similar), adicionar:

```tsx
  const [fonte, setFonte] = useState<'pdf' | 'api'>('pdf')
```

- [ ] **Step 2: Mandar `fonte` no body da geração**

Localizar o `fetch('/api/kpi/beta'` com `body: JSON.stringify({ ... })` e incluir `fonte`:

```tsx
        body: JSON.stringify({ escalaBucketPaths, unitracBucketPaths, data, alteracoes, fonte }),
```

- [ ] **Step 3: Renderizar o toggle**

Perto do seletor de data/arquivos, adicionar o controle (usar as classes já existentes na tela pra botões; este é o padrão mínimo):

```tsx
        <div className="flex items-center gap-2">
          <span className="text-xs font-semibold uppercase tracking-wider text-[var(--color-fg-muted)]">Fonte</span>
          <button type="button" onClick={() => setFonte('pdf')}
            className={cn('px-2 py-1 rounded text-xs font-medium border', fonte === 'pdf' ? 'border-[var(--color-success)] bg-[var(--color-success-soft)] text-[var(--color-success-soft-fg)]' : 'border-[var(--color-border-strong)] text-[var(--color-fg-muted)]')}>
            PDF
          </button>
          <button type="button" onClick={() => setFonte('api')}
            className={cn('px-2 py-1 rounded text-xs font-medium border', fonte === 'api' ? 'border-[var(--color-success)] bg-[var(--color-success-soft)] text-[var(--color-success-soft-fg)]' : 'border-[var(--color-border-strong)] text-[var(--color-fg-muted)]')}>
            🛰️ API (sem PDF)
          </button>
        </div>
```

- [ ] **Step 4: Quando fonte==='api', não exigir o PDF na UI**

Se a tela bloqueia o botão "Gerar" sem arquivo Unitrac, afrouxar: o botão deve habilitar com escala + data quando `fonte === 'api'`. Localizar a condição `pronto`/`disabled` do botão gerar e adicionar `|| fonte === 'api'` na parte que exige o Unitrac. (Conferir a variável exata no componente; manter a exigência de escala + data.)

- [ ] **Step 5: Verificar tipos e lint**

Run: `npx tsc --noEmit 2>&1 | grep "^src/" | head` → sem saída.
Run: `npx eslint src/app/painel/kpi/beta/page.tsx 2>&1 | grep -E "error" | head` → sem erros (warnings pré-existentes ok).

- [ ] **Step 6: Commit**

```bash
git add src/app/painel/kpi/beta/page.tsx
git commit -m "feat(kpi-beta): toggle Fonte PDF|API na tela beta"
```

---

### Task 6: Validação ponta a ponta no beta

- [ ] **Step 1: Subir o dev server e gerar pela API**

Run: `npm run dev` (ou o script de dev do projeto). Abrir `/painel/kpi/beta`, subir a escala do dia, selecionar Fonte = API, gerar. Conferir que os 3 veículos-caso (RJN, FUM, LKR) aparecem entregues com horários coerentes.

- [ ] **Step 2: Comparar com o modo PDF**

Gerar o mesmo dia com Fonte = PDF e comparar o preview lado a lado. Divergências grandes = bug de consolidação a investigar (voltar à Fase 1). Pequenas (±min) são esperadas.

- [ ] **Step 3: Abrir PR**

```bash
git push -u origin feat/kpi-api-only-beta
gh pr create --title "feat(kpi-beta): gerar KPI 100% pela API do Unitrac (sem PDF)" --body "Engine de consolidação (eventos GPS crus → paradas) + toggle Fonte PDF|API no beta. Gate Fase 1: <colar números do comparador>. Produção intocada. Testes da engine em consolida.test.ts."
```

- [ ] **Step 4: Merge após revisão**

```bash
gh pr merge --merge
```

---

## Notas de calibração (referência da Fase 1)

Se o gate não bater, os botões de ajuste, em ordem de impacto:
1. **`RAIO_CLUSTER_M`** (150m): muito baixo fragmenta uma entrega em vários clusters; muito alto funde lojas vizinhas. Para lojas-gêmeas (~80m) não passar de ~100m.
2. **Inferência de saída**: hoje saída = chegada do próximo cluster. Se inflar duração (caminhão dormiu na loja → próximo cluster só de manhã), limitar pela janela da rede ou pelo último evento + folga.
3. **`MIN_DUR_SEM_GEO_SEG`** (300s): sobe pra cortar mais blips de trânsito; desce se entregas reais sem geofence estiverem sendo descartadas.
4. **`RAIO_BASE_M`** (500m): ajustar se paradas perto da base forem classificadas errado.
